### Service vs Intent Service vs Bound Service

```
Intent's and Service Intent's are activity independent.
While Bound Intent's destroys when activity terminates.

Intent Service creates thread automatically for us while
we need to create our own thread for Service
```